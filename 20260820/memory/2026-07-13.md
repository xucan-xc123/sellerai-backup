# 2026-07-13 开发日志

## AI 电影级视频智能体 — 阶段1~4 全部完成

### 阶段1：工程初始化
- Electron + Vite + Vue3 + TailwindCSS 骨架搭建
- 四区布局（320px 左侧素材面板 / 中间编辑区 / 360px 右侧预览渲染面板 / 顶部 Header）
- 封装修通用组件：StatusTag、FileUpload、TextField、TextArea、SelectField、CollapsePanel、ShotCard、DialogBase、Icon
- 全局规范：Fixed 色值 #94ccff、圆角 6/8px、浅色办公风、无动画

### 阶段2：核心链路打通
- 分镜自动生成（接入火山方舟豆包 API，doubao-seed-evolving 模型，close thinking）
- HyperFrames CLI 渲染（文生视频框架，ffmpeg + Puppeteer 后端）
- 端到端实测通过：生成6镜→渲染12秒 1280×720→成片 658.9KB
- HyperFrames composition cwd 拼接 bug 修复（cwd 指向文件所在目录，传相对路径）
- Electron v31.7.7 整链编译 24 模块零错误

### 阶段3：底层重构（密钥安全 + IPC 归一 + 持久化目录）
- **密钥安全**: API Key AES-256-CBC 加密存 E盘 → 前端代码零明文; 旧 D盘密钥首次启动自动迁移
- **全量 IPC 归一**: 13 条标准化通道（素材 open/delete/thumb、渲染、工程存档、生成分镜、导出、模板），渲染层零 API/零本地 IO
- **持久化目录**: 统一迁至 `E:\QClaw\Work-QClaw\AI电影级视频智能体\data\{projects,templates,renders}`; 每次生成/渲染自动归档 auto-latest.json（含版本回溯）
- **真实端到端自测**: 5/5 全过（联网生成6镜→渲染1080p→工程存档→E盘成片658.9KB）

### 阶段4：分镜配套文生图模块（新增，保留全部现有代码）
- 每条 ShotCard 新增【AI 生成镜头图】按钮 + 画面预览/放大/删除
- ShotImageDialog 参数弹窗（正向/反向提示词、尺寸下拉 9:16/16:9/1:1、生成数量 1~4）
- CenterPanel 批量生成全部分镜画面 + 比例选择
- LeftPanel 联动开关（自动生图）+ ModelScope Token 加密存储
- 4 条 IPC 通道（generate-shot / batch-generate / delete-shot-img / get-local-img-path）
- 落盘 `D:/VideoProject/Source/ShotImg/{projectId}/`
- HyperFrames composition 优先用本地图作画面背景
- 序列化安全：toPlain 剥离 Vue Proxy，修复 "An object could not be cloned"
- 低配模式限制单次最多2张
- 23/24 静态验证 + 启动自测通过（仅目录存在性检查依赖启动后才通过）
- 云图文生图（ModelScope Z-Image-Turbo）代码已接好，需用户填 ModelScope Token 方可调用

### 已知待办
- ModelScope Token 未提供，生图链路无法真实联调（架构代码完整）
- 工程版本回溯 UI 未接入
- 缩略图网格/加载工程回填 projectId 未做
- UI 观感集中打磨未做

## 依赖/工具
- Node v22.22.3, npm 10.9.8, Electron v31.7.7
- HyperFrames v0.7.56 (global, `C:\Users\Administrator\AppData\Roaming\QClaw\npm-global\node_modules\hyperframes`)
- 火山方舟豆包 API (doubao-seed-evolving)
- ModelScope Z-Image-Turbo 待接入
- 镜像源：npmmirror / npm config set prefix
- FFmpeg v8.1.2

## C盘清理
- `C:\Users\Administrator\.openclaw\` → 内容复制到 `D:\AIAgent\.openclaw\`（但 prefix 被 NPM_CONFIG_PREFIX 环境变量覆盖无法改）
- Python 包升级：16个包全部升到最新（含 torch 2.13.0, transformers 5.13.1→回退4.57.3 因 qwen-tts 冲突）
- 密钥迁移：旧 D盘 → 新 E盘加密目录

## E盘持久化目录结构
```
E:\QClaw\Work-QClaw\AI电影级视频智能体\data\
├── apikey.enc          # 火山方舟 Key（AES加密）
├── mstoken.enc          # ModelScope Token（AES加密）
├── projects/            # 工程存档 JSON
├── templates/           # 分镜模板
└── renders/             # 成片输出 MP4
```
