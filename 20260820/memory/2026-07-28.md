# 2026-07-28

## SellerAI 部署与修复

### QuotaBadge 误判（19:18）
- 用户反馈登录后 listing 页面右上角有黄色"3天免费试用"框体
- 排查：`QuotaBadge.tsx` 中 free 状态未登录显示 `Free · 3 left today`，登录后用户看到数字"3"以为是"3天"
- 初始修复：颜色从黄#f59e0b改灰#9ca3af，文案改 `Free Plan`
- 创始人怒批：没用直接删
- 最终：整个 QuotaBadge 组件从 `/app/page.tsx` 删除，所有引用清除，删除 `QuotaBadge.tsx`
- 构建+部署成功（version ID: 87edbfba）

### AutoCS 占位页问题（~19:25）
- 用户问 AutoCS 是否只有网页，怎么体现功能
- 查实：AutoCS 从来没有真正开发过——只有一个纯文字占位页面 + `NEXT_PUBLIC_AUTOCS_URL` 条件按钮
- 占位文字存在于两个地方：`app/tools/automation/page.tsx`（独立路由）和 `/app/page.tsx` 内联的 `AutomationDashboardTab` 组件
- 定价 $29.9/月宣传三件套，实际只有 Listing + Keyword 能用
- 给创始人三个选项：A)降价两件套 B)尽快建 MVP C)保留占位改Coming Soon

### 项目状态
- 产品：Listing(可用) + Keyword(可用) + Automation(空占位)
- 定价策略需创始人拍板
- QuotaBadge 已彻底去除
